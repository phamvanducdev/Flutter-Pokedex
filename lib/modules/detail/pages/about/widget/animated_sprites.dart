import 'package:flutter/material.dart';

class AnimatedSpritesWidget extends StatelessWidget {
  const AnimatedSpritesWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
